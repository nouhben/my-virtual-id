name: best_starter_architecture
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^1.1.2
  
  cupertino_icons: ^1.0.0
  
  firebase_core: ^0.7.0
  cloud_firestore: ^0.16.0
  firebase_auth: ^0.20.0+1

  flutter_launcher_icons: ^0.8.1
  flutter_svg: ^0.18.0
  google_fonts: ^1.1.2

  provider: ^4.3.3
  riverpod: ^0.12.4

  shared_preferences: ^0.5.12+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
